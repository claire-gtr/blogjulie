<div class="container">
  <%= simple_form_for(@post) do |f| %>
    <%= f.input :title, label: "Titre" %>
    <%= f.input :content %>
    <%= f.button :submit, value: 'Editer cet article'%>
  <% end %>
</div>
