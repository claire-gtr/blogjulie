<div class="container">
  <%= simple_form_for(@post) do |f| %>
    <%= f.input :title, label: "Titre" %>
    <%= f.input :content %>
    <%= f.button :submit, value: 'Publier cet article'%>
    <% end %>


  <%= link_to "Retour à l'accueil", root_path %>
</div>
